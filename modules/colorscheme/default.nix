{
  config = {
    colorschemes = {
      rose-pine = {
        enable = false;

        settings = {
          variant = "moon";
          dark_variant = "moon";
          dim_inactive_windows = true;
        };
      };

      dracula-nvim.enable = true;
    };
  };
}
